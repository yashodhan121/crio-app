extends layout

block content
	.container-fluid
		center
			.container1
				if username
					img.image(src=username,onerror="this.src='/img/avatar.png'", alt='Avatar')   
				.overlay
					a(href="/users/fileupload" style="text-decoration:none;").text CHANGE

			h1.prtex #{user.name}
